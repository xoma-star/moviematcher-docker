version: '3.9'

services:
  server:
    build: ./server
    restart: unless-stopped
    depends_on:
      - pocketbase
      - redis
    ports:
      - '3000:3000'
    networks:
      - app-network
  pocketbase:
    build: ./pocketbase
    ports:
      - '127.0.0.1:8090:8090'
    volumes:
      - pocketbase-data:/pb_data
    networks:
      - app-network
  redis:
    image: redis
    ports:
      - '6379:6379'
    networks:
      - app-network
  nginx:
    image: nginx
    depends_on:
      - server
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
    ports:
      - '80:80'
      - '443:443'
    restart: unless-stopped
    networks:
      - app-network
  certbot:
    image: certbot/certbot
    depends_on:
      - nginx
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
    command: certbot --nginx --email sasha.sirotyin09@gmail.com --agree-tos --no-eff-email --staging -d api.xoma-star.tk -d pb.xoma-star.tk
    networks:
      - app-network
volumes:
  certbot-etc:
  certbot-var:
  pocketbase-data:

networks:
  app-network:
    driver: bridge